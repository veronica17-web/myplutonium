const demo = '          FunctionUp             '
let trimming = demo.trim()
let lowwer = demo.toLowerCase()
let upper = demo.toUpperCase()

let changes = function(){
    console.log(  trimming+ lowwer + upper)
}

module.exports.fromatterchanges = changes